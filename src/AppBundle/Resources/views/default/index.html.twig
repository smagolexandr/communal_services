{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
           <h1>
               Welcome
           </h1>
            <a href="{{ path('login') }}">
                Sign in
            </a>
            <a href="{{ path('registration') }}">
                Sign up
            </a>
        </div>
    </div>
{% endblock %}